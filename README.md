# Resume

LaTeX 简历，push 后由 GitHub Actions 自动编译。

## 用法

1. 编辑 `main.tex`
2. commit 并 push 到 `main` 分支
3. GitHub Actions 自动编译，把最新的 **`Resume.pdf`** 提交回仓库根目录

## 下载 PDF

- 直接下载仓库根目录的 [`Resume.pdf`](./Resume.pdf)（始终是最新版）
- 或到 [Actions](../../actions) 页面，打开最近一次运行，下载 `Resume` artifact
